//STL CONTAINERS
/*
==> SEQUENCE CONTAINERS ==>> linear fashion >> vector(fast random access)[insertion deletion middle slow insertion at the end fast] , list(random access slow)[insertion deletion middle  last fast ] , dequeue
==> ASSOCIATIVE CONTAINERS ==>> direct access >> map , multimap , set , multiset (FAST ACCESS) (TREE LIKE STRUCTURE)  ==>> (all operation fast except random access)
==> DERIVED CONTAINERS ==>> real world modelling >> stack , queue , priority queue ==>> (depends) ==> data structure
*/

#include<iostream>
using namespace std;

int main()
{
    return 0;
}